import 'package:flutter/material.dart';

class ManageFeedback extends StatelessWidget {
  const ManageFeedback({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Feedback"));
  }
}